# Parse Core

Placeholder for Phase 2 native parsing.

Future implementations can provide Rust FFI bindings for zero-copy JSON repair
and partial streaming recovery while satisfying the Go gateway `IdeaParser`
interface.
